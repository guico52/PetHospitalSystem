<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guico.mapper.PetMapper">
    <select id="selectById" resultType="com.guico.pojo.Pet">
        SELECT *
        FROM pets
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="list">
        SELECT *
        FROM pets
    </select>

    <delete id="deleteById" parameterType="int">
        DELETE
        FROM pets
        WHERE id = #{id}
    </delete>

    <insert id="insertPet" parameterType="com.guico.pojo.Pet">
        INSERT INTO pets(id, name, birth_date, type_id, owner_id)
        VALUES (#{id}, #{name}, #{birthDate}, #{typeId}, #{ownerId})
    </insert>

    <update id="updatePet" parameterType="com.guico.pojo.Pet">
        UPDATE pets
        SET name = #{name}, birth_date = #{birthDate}, type_id = #{typeId}, owner_id = #{ownerId}
        WHERE id = #{id}
    </update>


</mapper>